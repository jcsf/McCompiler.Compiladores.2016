/*
#include <stdio.h>
*/

int main2(int argc, char** argv) {
	char buffer[20];
	char* buf;
	int** int_buffer;
	char a;
	
	/*
	int a;
	a = buffer[1] + buf[2];
	puts(argv[0]);
	argv[2];
	*/

	/*
	*buffer;
	*buf;
	buffer[2];
	buf[4];
	*(buffer+2);
	*(buf + 2);
	*(*(int_buffer + 5));
	*/

	*(buffer+a);

	buf[a];

	return 0;
}